// Service worker source for spomky-labs/pwa-bundle.
// The bundle replaces the placeholders below at build time with the Workbox import,
// the standard caching rules (assets, images, fonts…) and the offline fallback.
//WORKBOX_IMPORT_PLACEHOLDER

//STANDARD_RULES_PLACEHOLDER

//OFFLINE_FALLBACK_PLACEHOLDER
